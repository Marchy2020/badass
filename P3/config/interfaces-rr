
# =========================
# CONFIGURATION CLASSIQUE (/etc/network/interfaces)
# =========================
# Déclaration de l'interface loopback (toujours UP, sert de router-id)
auto lo
iface lo inet static
    address 10.0.0.100         # Adresse IP du loopback (utilisée comme router-id BGP/OSPF)
    netmask 255.255.255.255    # Masque /32 = une seule IP

# Déclaration de l'interface physique principale (réseau underlay)
auto eth0
iface eth0 inet static
    address 10.0.0.100         # Adresse IP de l'interface eth0 (réseau de transport)
    netmask 255.255.255.0      # Masque /24 = réseau 10.0.0.0/24

# =========================
# CONFIGURATION MANUELLE (depuis la console GNS3 ou Docker)
# =========================
! ip addr add 10.0.0.100/32 dev lo         # Ajoute l'IP loopback (router-id)
! ip link set lo up                       # Met le loopback UP
! ip addr add 10.0.0.100/24 dev eth0      # Ajoute l'IP sur eth0
! ip link set eth0 up                     # Met l'interface eth0 UP